/**
 * 数据统计
 */

export function $g(eventName: string, eventParams?: ObjAny) {
  // try {
  //   console.log(`gtag("event", "${eventName}", `, eventParams);
  //   gtag("event", eventName, eventParams);
  // } catch (e) {
  //   console.error(e);
  // }
}
